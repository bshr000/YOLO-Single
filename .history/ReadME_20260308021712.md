# YOLO-Single

基于 PyTorch 的单模态目标检测项目，整体思路参考 YOLO 系列，当前实现包含训练、验证（mAP）和推理可视化三条主流程。

## 1. 仓库介绍

本项目面向 YOLO 格式数据集（`class cx cy w h`，归一化坐标），支持：

- 多类别目标检测
- 可切换骨干网络（如 `resnet18/resnet34/resnet50/cspdarknet_*`）
- PAN-FPN 特征融合
- DFL 回归分支与 Anchor-Free 风格预测
- 训练阶段 EMA、AMP、梯度裁剪

核心脚本说明：

- `train.py`：模型训练
- `val.py`：验证评估（mAP@0.5 与 mAP@0.5:0.95）
- `inference.py`：离线推理与结果保存
- `dataset.py`：YOLO 数据集读取与增强
- `model.py`：模型结构定义
- `loss.py`：检测损失
- `utils.py`：通用工具函数（checkpoint、NMS 等）
- `config.yaml`：训练与数据配置

## 2. 环境安装

### 2.1 Python 环境

建议：

- Python `3.9+`
- PyTorch `2.0+`
- CUDA（可选，GPU 训练推荐）

### 2.2 安装依赖

```bash
pip install -r requirements.txt
```

如需在训练时统计 GFLOPS，可额外安装：

```bash
pip install thop
```

### 2.3 依赖列表

项目当前依赖（见 `requirements.txt`）：

- `torch`
- `torchvision`
- `numpy`
- `opencv-python`
- `Pillow`
- `PyYAML`
- `tqdm`
- `matplotlib`

## 3. 数据及数据结构

### 3.1 目录结构（推荐）

你可以按下面的方式组织数据：

```text
your_dataset/
  images/
    train/
      000001.jpg
      000002.jpg
    val/
      000101.jpg
  labels/
    train/
      000001.txt
      000002.txt
    val/
      000101.txt
```

### 3.2 标签格式

每个标签文件为 YOLO 标准格式，一行一个目标：

```text
class_id center_x center_y width height
```

其中坐标均为相对图像宽高归一化到 `[0, 1]` 的浮点数。

### 3.3 配置文件

在 `config.yaml` 中修改数据路径与类别数：

- `data.train_images`
- `data.train_labels`
- `data.val_images`
- `data.val_labels`
- `data.num_classes`

> 注意：当前示例配置使用了 Windows 绝对路径，请在本机按实际路径修改后再运行。

## 4. 命令

### 4.1 训练

```bash
python train.py --config config.yaml
```

训练权重会保存在 `config.yaml` 中 `train.checkpoint_dir` 指定的目录下。

### 4.2 验证（mAP）

```bash
python val.py --config config.yaml --checkpoint checkpoints/best_model.pth
```

可选参数：

- `--conf-thres`：置信度阈值（默认 `0.001`）
- `--nms-thres`：NMS 阈值（默认 `0.6`）
- `--verbose`：输出各类别指标

### 4.3 推理

```bash
python inference.py --source path/to/images --config config.yaml --checkpoint checkpoints/best_model.pth --output inference_output --save-txt
```

可选参数：

- `--conf-thres`：置信度阈值（默认 `0.25`）
- `--nms-thres`：NMS 阈值（默认 `0.45`）
- `--save-txt`：保存每张图片的检测结果文本

## 5. 结果示例

### 5.1 指标示例（模板）

你可以将实验结果整理为表格放在这里：

| Model | Input Size | Params (M) | GFLOPS | mAP@0.5 | mAP@0.5:0.95 |
|------|------------|------------|--------|---------|--------------|
| YOLO-Single (CSPDarknet-S) | 640 | - | - | - | - |

### 5.2 可视化示例（模板）

建议在仓库中新建 `assets/` 目录保存示例图，然后在 README 中引用：

```markdown
![det-demo-1](assets/det_demo_1.jpg)
![det-demo-2](assets/det_demo_2.jpg)
```

---

如果你愿意，我可以继续帮你补一版更“论文风格”的 README（包含模型结构图、损失函数描述和训练超参数表）。  
